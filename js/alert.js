const form = document.querySelector("[data-form]");

form.addEventListener("submit", (evento) => {
  evento.preventDefault();

    alert("Recuerda que te encuentras en el sitio de administrador.");
  
});